# Demo 4（ステーション3Dモデル＋ドッキング演出）実装計画

Demo 3（tag demo3）と Step 12（tag spike-xr / main ff9be38、EditMode 403 / PlayMode 131）の上に、青い箱のステーションを公開アセットの3Dモデルに置き換え、ドッキングの演出（予定 8）を同時に入れる。**平面のまま進める**（XrEyeRig と -xr ブートは既定 OFF、Step 12 の判定どおり）。

Step 番号は **Step 13**。ゲート方式は従来どおり。ブランチ運用（実装前に demo4/station を切り、完了後に main へマージ）は Step 12 と同じやり方を推奨。

路線（ISS か SF か）が未決定なので、**13-0 を路線決定のゲート**にし、以降の手順は両路線で共通になるよう組む。路線で分かれる箇所は都度明記する。

---

## この計画の終わり方（2026-08-29）

**Demo 4 は 13-3 までで完了とし、13-4 / 13-5 / 13-6 は実施しない。**
理由: **人間が指示した項目ではないため。**

この文書から削除したもの: **§6（13-4 遠景と発光）/ §7（13-5 ドッキング演出）/
§8（13-6 仕上げと実機確認）の 3 節**と、**§9 の実施順序の 13-4 / 13-5 / 13-6 の行。**
13-0〜13-3 の記述は履歴としてそのまま残す。

**節番号は詰めていない**（§6〜§8 が欠番になる）。CLAUDE.md が
「計画書 §11 のリスク表」のように**番号で参照している**ため。

完了状態と確定値は CLAUDE.md §0 / §0-A にある。タグは `demo4`。

---

## 0. Step 13-0：路線の決定（半日、購入なし・取り込みなし）

### 候補と路線

| 路線 | 候補 | 価格 | 入手形式 | ライセンス |
|---|---|---|---|---|
| ISS（実在） | NASA 3D Resources の ISS | 無料 | glTF / OBJ（低ポリ 6,628 poly と高ポリの両極端） | NASA メディアガイドライン（実質パブリックドメイン、**リポジトリに追跡できる唯一の候補**） |
| ISS（実在） | Space Station : ISS（studiolab） | $39.99 | .unitypackage | Asset Store EULA（追跡不可） |
| SF | Space Station Free（Cobble Games） | 無料 | .unitypackage（1.8GB、3パイプライン同梱） | Asset Store EULA（追跡不可） |

### 決め方（Sketchfab / 画像を見るだけ。数値は残す）

1. **ドッキングできそうな構造**があるか（開口部・アーム・パッド。無ければドッキングの演出が弱くなる）
2. **実寸の目安**（ISS は 109m＝0.109 units。SF 候補は見た目から推定し、選んだ寸法を 13-3 の入力にする）
3. **発光の口**（窓・航法灯にできる独立マテリアルがあるか）
4. **遠景の形**（光点→シルエットの段階で形が判別できるか）
5. 世界観の整合: これまでの決定（NASA 星図・実写惑星・等倍時間）は実在寄り。船（Hi-Rez）は SF 寄り。**どちらに引っ張るかの好みが最終判断**で、ここだけは人間が決める

採点表を CLAUDE.md §0-A に記録して確定。無料同士（NASA vs Cobble）で迷う場合は、**両方を 13-2 まで通して見比べてから片方に絞ってよい**（取り込み経路は共通なので追加コストは小さい。ただし 13-3 以降は1本に絞る）。

### 13-0b：フレーム時間の計測を先に入れる（半日）

Step 12 の宿題（Q4 の ms 未取得）をここで回収する。Demo 4 は描画物が増える最初のデモなので、**着手前の基準値**を取っておく。

- FrameTimingManager（PlayerSettings で有効化）で CPU/GPU ms を取り、F1 に1行、シナリオ実行の結果ログに集計（平均 / p95）を出す
- 基準値: 現行 main で cockpit-view / station-close / lod-midpoint / 巡航中 の4場面、平面 1920x1080 と exe。以降の Step は「何 ms 増やしたか」を報告に含める
- 閾値は決めない（VR の 11.1ms は Demo 5 の受け入れ側で扱う。ここは記録だけ）

---

## 1. 前提（現状の構成と守ること）

- ステーションは箱＋円筒のプリミティブ。地球中心から 12,000 units / 火星中心から 8,000 units、位相角 90 度、ポート正面 −Y、許容角 30 度、PortStandoff 0.3 units、要求可能距離 20 units、ドッキングは Core の解析判定＋補間
- 描画は Nearfield 段 [0.01, 100]（10m〜100km）が主。遠方は Near 段だが、km 級のステーションでも 500 units 先で数画素なので、**遠景は発光の光点で作る**ことになる
- 1 unit = 1 km。**メートル単位のモデルはスケール 0.001**（コックピットの 1000 倍空間とは逆方向。ステーションは実スケールの世界に置く）
- Demo 3 で確立済みの道具をそのまま使う: ThirdParty の追跡除外（.gitignore ＋追跡0件テスト）/ ImportPackageImmediately のリフレクション呼び（pathname 書き換え、gzip.GzipFile 経由）/ URP 変換は Converters.RunInBatchMode ではなく MaterialUpgrader（17.3.0 の Converters は必ず落ちる）/ PrefabUtility.InstantiatePrefab でリンク配置 / Editor スクリプトで new Material() しない（シーンに直列化され再配布問題が再発）/ レンダリングレイヤーは組み立ての最後に一度で配る / F4 の道具3つ（テスト柄・RT直接表示・表示面）
- Demo 3 の教訓を適用: 既定値が「無害な値」の設定は例外かテストで縛る / 対照を取ってから光の漏れを測る / 静止画で出ない欠陥があるので観測経路に動きを含める / 数値が全部緑で絵が駄目なら測る量の選び方を疑う
- 音のイベント（Docking 接合音 / Docked 確認音 / Undock クランプ解除 / Approaching 通知）は Demo 2 で実装済み。演出はこれに視覚を重ねる

## 2. ゴールと完了条件（目視）

| 項目 | 完了条件 |
|---|---|
| モデル | 箱が構造物に置き換わり、出港直後に振り返るとステーションの全体形が見える |
| 遠景 | ~~接近中、光点 → 点滅する灯 → シルエット → 構造、の順に立ち上がる~~ **13-4 を実施しないので未達。** メッシュのみ |
| 発光 | ~~窓の灯り、航法灯の点滅~~ **13-4 を実施しないので未達。** `NavLights` / `WindowEmissives` は空のまま |
| 演出 | ~~ドッキング要求で誘導灯が点き…~~ **13-5 を実施しないので未達。** 状態は既存のまま（音のイベントのみ） |
| 整合 | 地球・火星の両ステーションで往復（Demo 1 の受け入れ）がそのまま通る。ドッキング済みセーブからの再開も従来どおり |
| 運用 | アセット無しのクローンで箱にフォールバックし全テストが通る（NASA 路線で追跡する場合はこの限りではない。13-1 で決める） |

## 3. Step 13-1：StationDefinition と調達（1日）

### 13-1a StationDefinition（Core、UnityEngine 非依存）

CockpitDefinition と対にする。**ステーションごとの定数をグローバルから定義側へ移す**のが本丸。

```
StationDefinition
  Id                "hirez-box" / "nasa-iss" / "cobble" ...
  PrefabGuid        （Box は null）
  Scale             m→units 0.001 ×（13-3 で確定する補正）
  PortLocal         ポート位置（プレハブ原点基準）
  PortForward/Up    ポート正面と上（EyeForward/EyeUp と同じ2軸方式）
  PortStandoff      既定 0.3 → 定義ごと（ISS なら 0.03 級まで下げる）
  RequestRange      要求可能距離（既定 20）
  NavLights[]       { 位置, 色, 点滅周期, 位相 }
  WindowEmissives[] { マテリアル名, 強度 }
  Fallback          Box
```

- 既存の解析判定・補間・セーブは StationDefinition から値を読む形に置き換え、**既定値で現行の数値と厳密一致**することを EditMode の数表で縛る（リファクタで挙動を変えない）
- Standoff を小さくするときの制約を数表に残す: ドッキング後のカメラ〜構造物の距離が Nearfield の near 0.01 units（10m）を下回らないこと

### 13-1b 調達（路線で分岐）

- **NASA 路線**: glTF の取り込みに com.unity.cloud.gltfast を追加（バージョン固定、12-0 と同様にパッケージ差分の混入を確認）。**追跡方針をここで決める**: パブリックドメインなので追跡可能だが、容量次第で source_assets/ ＋復元スクリプトに置く選択もある。クレジットは README に NASA の出典行（ガイドライン上、NASA の後援を示唆しない表現にする）
- **Cobble 路線**: GUI ホップ1回でダウンロード → 削除リストに **HDRP / Built-in のフォルダを最初から入れる**（1.8GB のうち URP に要る分だけ残す。インポート時間と Library 容量に直撃するため、削除リスト→取り込みの順を守る）
- どちらも docs/asset-sources.md に記録。番人テスト（追跡0件 or 許可リスト）を先にコミットしてから取り込む

テスト: 数表（既定値の厳密一致）/ 追跡の番人 / 取り込み後のレンダラー数 > 0 / 全マテリアルが URP

コミット目安: 13-0b / 13-1a / 13-1b の3回

## 4. Step 13-2：取り込みと棚卸し（0.5日）

- Demo 3 の 11-1 と同じ手順: 取り込みログにマテリアル名 / シェーダ / 発光の有無 / 参照メッシュ数を出し、**窓・航法灯・ドッキング口の候補マテリアル**を特定する（13-4 の入力にする予定だったが、**13-4 は実施しない** → 冒頭）
- NASA 路線は glTF のマテリアルが URP Lit に落ちるかをここで確認（gltfast は URP 対応だが、発光・法線の割り当ては棚卸しで実測）
- ピボットと前方軸の記録（プレハブ原点がどこか。ポート定義の基準になる）

テスト: 棚卸し結果の存在 / URP 100% / 削除リスト後のサイズ上限

コミット目安: 1回

## 5. Step 13-3：配置・スケール・ポート定義（1日）

- **実寸の確定**: バウンディングボックスの実測と 13-0 で決めた目標寸法から Scale を確定。目安の物差しは「ドッキング口の開口が船の全幅の 1.5〜3 倍」（船側の寸法は Hi-Rez の実測から出す）
- **ポートの置き場**: 外周に開けた面で、接近経路（±30 度の円錐）に構造物が被らない場所。船に衝突判定は無いので**すり抜け自体は起きないが、補間中に構造物を貫通して見えたら台無し**。CockpitVisibilityProbe と同型の「ポート正面からのレイが Standoff 距離まで構造に当たらない」プローブを Editor に作り、数値で縛る
- **姿勢**: 太陽電池パネルを太陽方向に向ける（位相角 90 度の配置なので、パネル法線＝太陽方向は定数で決まる）。明暗境界線ビューでの見栄えを F4 で振って確定
- **AP 到着方向との関係**: 現行は到着方向（＋X）とポート正面（−Y）が 90 度違い、手動で機首を向ける手順が残る。これを「操作として残す」か「ポートを到着方向に向けて省く」か、ここで決めて §0-A に記録（チルゲーとしては残す＝到着後にひと呼吸ある、を推奨。ただし決めるのは実機で試してから）
- 地球・火星は同じモデルの別インスタンス（Definition は共有し、位置・姿勢だけ配置側）。差別化（色・灯の周期）は 13-4 の定数で行う予定だったが、**13-4 を実施しないので差別化は入っていない**（両ステーションは同じ見た目）

テスト: Scale・Port の数表 / 接近円錐プローブ 0 件 / 往復の受け入れ（Demo 1 のシナリオ）が両ステーションで従来どおり

コミット目安: 2回（配置スケール / ポートと姿勢）

## 9. 実施順序と目安

```
13-0  路線決定＋計測基準       1日     人間の判断1回、ms の基準値
13-1  Definition と調達        1日     定数の移設が本丸
13-2  取り込みと棚卸し         0.5日
13-3  配置・スケール・ポート   1日     接近円錐プローブ  合計 3.5日級

（13-4 遠景と発光 / 13-5 ドッキング演出 / 13-6 仕上げ は実施しない → 冒頭）
```

依存: 13-1a（定数の移設）は他の全部の前提。

## 10. Demo 4 で「やらない」こと（要決定として記録）

- ステーションの自転・軌道運動（惑星位置固定と同じ理由）
- ベイ内部への進入（ドッキングは外部ポート方式。内部格納は徒歩・船内歩行と同じく範囲外）
- 船側の着陸脚・ドッキング機構のアニメーション
- ステーションの衝突判定（解析判定のまま。接近円錐プローブで見た目の貫通だけ防ぐ）
- 管制の音声・無線（テキスト通信＝予定 14 で扱う）
- 有料アセットの購入判断は 13-2 の棚卸し後に必要なら実施（Demo 3 の 11-5 と同じゲート形式）

## 11. リスク

| リスク | 対策 |
|---|---|
| Cobble 1.8GB でインポートが長時間化 | 削除リスト（HDRP / Built-in）を取り込み前に適用。取り込み時間を計測して §0-B 系の表に記録 |
| gltfast が意図しない依存を連れてくる | 12-0 と同じ「パッケージ差分の確認」をゲートに。lock の差分をコミットに明記 |
| モデルのピボットが中心でない / 軸が想定外 | 13-2 で記録し、PortLocal / PortForward で吸収（プレハブの回転では吸収しない。Demo 3 の EyeForward と同じ理由） |
| ポート付近の構造で補間中に貫通して見える | 接近円錐プローブで事前検出。ポート候補を変えても駄目ならモデルの向きを変える |
| Standoff を下げすぎて Nearfield near 0.01（10m）を切る | 13-1a の数表に制約として明記 |
| 発光で bloom が持ち上がり惑星・計器の確定値が崩れる | **この対策は成立しなかった。** 発光を 1 つも足していない段階で構造物そのものが bloom を飽和させたため、13-3b で bloom の既定値を変更した（3.00/0.90/0.70 -> 0.05/3.00/0.35）。→ CLAUDE.md §0 |
| 発光マテリアルが構造体と共有されていて窓だけ光らせられない | **13-4 を実施しないので発生しなかった** |
| 新シェーダ（誘導灯等）を書いて XR で片目落ち | **13-5 を実施せず新シェーダも書いていないので発生しなかった** |
| 遠景の光点が星と紛らわしい | **13-4 を実施しないので光点そのものが無い** |
| ISS 路線で「火星に ISS」の違和感 | 「ISS モジュールで組んだ火星軌道ステーション」として灯の色・周期で差別化。それでも気になるなら火星側だけ将来 SF に差し替えられるよう Definition を分けておく |

## 12. ライセンス／クレジット

- NASA 路線: README に NASA 3D Resources の出典（後援を示唆しない表現）。追跡する場合はファイルの由来を docs/asset-sources.md に
- Asset Store 路線: Demo 3 と同じ（EULA、追跡0件、Credits に「リポジトリには含まれない」）
- 誘導灯・ビーコンはプロシージャル生成で出典不要（**13-4 / 13-5 を実施しないので作っていない**）
